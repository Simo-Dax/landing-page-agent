---
description: Lancia il copy-agent — copy di conversione per blocco, in voce brand, anti-AI
argument-hint: [progetto o sezione]
---

# /copy

Avvia il `copy-agent` (Fase 6). Step indipendente.

Input: $ARGUMENTS

## Cosa fare
1. Leggi architettura/wireframe approvato, brief, `buyer_personas.md`, `context/brand/tone_of_voice.md`, `context/brand/anti-ai-writing-style.md`.
2. Leve con `marketing-psychology`; scrittura con `copywriting` + directives 02/10; edit/humanize con `copy-editing` + anti-ai.
3. Output: `output/<slug>/copy/copy_<sezione>.md` per blocco, message match con la sorgente di traffico.
4. Zero em dash, zero pattern AI. Numeri/social proof solo dal contesto o dalla sorgente fornita dall'utente, altrimenti `[DA VERIFICARE]`. Se l'area membri ha contenuto sorgente (es. checklist/CSV), traducilo e impaginalo in voce brand senza inventare.
